<div class="row">
    <div class="col-xs-8">
        <div class="mx-auto text-light font-weight-light h1 mt-5 mb-3">
            お気に入りデッキ
        </div>
    </div> 
</div>
<div class="row">
  <div class="col-xs-12 mt-5">
    <%= paginate @favorites %>
  </div>
</div>
<div class="row">
    <% @favorites.each do |favorite| %>
        <% deck=Deck.find_by(id: favorite.deck_id) %>
        <div class="col-xs-2 mt-3">
            <%= link_to image_tag("#{deck.imagefile_name}", :size =>'160x224'), deck_path(deck) %>
        </div>
   <% end %>
</div>
<div class="row">
  <div class="col-xs-12 mt-5">
    <%= paginate @favorites %>
  </div>
</div>